"""Pydantic data contracts (docs/PLAN.md §3): `TradeQuery`, the response
envelope, and `ErrorResponse`. These are the only shapes any node or route
is allowed to pass data around in — never positional args (master brief §3).
"""
