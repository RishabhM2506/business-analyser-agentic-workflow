"""Graph nodes (docs/PLAN.md §2.2). Every node here is deterministic Python
except `describe_item` and `summarize`, which call a model — "most nodes in
a good graph contain no AI at all" (the guide, Ch.3, cited in PLAN.md §2.2).
"""
