CURRENT_STEP: 3
CURRENT_ROLE: Software engineer (backend)
ITERATION: 1
GATE_STATUS: in_progress
LAST_COMPLETED: (1) Fixed a real curation bug: BCD's VERIFIED row for HS 120791 had a future-dated effective_from (my own error, spreading same-day updates across 3 calendar dates to satisfy a script constraint) - corrected via direct SQL, re-verified compute_landed_cost now correctly uses BCD=20% live. The calculation/narrative layer itself was already fully correct (VERIFIED-only, excluded_components listed, is_complete=False + landed_cost=None when incomplete, narrative says "unverified: X, Y, Z") - no code change needed there. (2) Completed the Tier-2 source investigation (Horticulture Varieties retry, DGFT, CBIC, FSSAI, FAOSTAT, World Bank) per the user's own report template, reported findings before building anything. Recommendations: Horticulture Varieties SKIP (real API works but returns yearly variety-certification counts, not variety names - not useful for query normalization); DGFT/CBIC/FSSAI REFERENCE-ONLY (no public APIs, CBIC also has an explicit anti-scraping notice); World Bank SKIP (real but only ~50-60 major commodities, no agricultural product-level granularity, per user's own direction). (3) Built and live-verified app/pipeline/faostat.py (BUILD, user-approved) - bulk-ZIP ingestion mirroring BACI's pattern, item-filtered (caller-supplied item_names, no hardcoded commodity - explicitly designed for the user's "tomorrow could be cement" requirement). Found and fixed two real bugs on the first live run: an asyncpg 32,767-parameter-limit violation (fixed with internal 1000-row batching) and a real UTF-8-vs-latin-1 encoding bug ("Türkiye" was corrupting to "TÃ¼rkiye"). Live-verified the core requirement: India+Poppy seed+Production is value=NULL/flag='M' for every year 2015-2024 (never a fabricated zero), while Türkiye has real values (flag='A'). New raw_faostat_records table/migration 9bfa1484a1d0. 14 new tests. 613 tests passing (was 599). mypy (64 files)/ruff/black clean.
NEXT_TASK: Two things pending, both from the user's latest message. (A) Not yet committed: everything from LAST_COMPLETED above needs a git commit - do that first. (B) Not yet started: the user's explicit request to design (not yet implement) a dynamic, LLM-driven source-selection mechanism - "the tool you are building we have to use them or not will be decided by llm because today we are going with reference of poppy seed tomorrow a search can be different like cement" - they want this DISCUSSED with them before implementation, not built directly. Need to: survey the existing architecture for how app/report/facts.py currently decides which sources apply to a given HS6 (currently: fully deterministic, every source is always queried for every HS6, with NOT_FOUND/NULL as the natural "doesn't apply" signal - e.g. Agmarknet already returns NOT_FOUND for poppy seeds without any special-casing) - then propose a concrete design for how an LLM would decide relevance (e.g., agriculture-specific sources like Agmarknet/MSP/FAOSTAT shouldn't even be queried for "cement", vs. commodity-agnostic sources like DGCIS/Comtrade/duty rates that apply to anything with an HS6) - and present this as a proposal for discussion, not a fait accompli implementation.
BLOCKED_ON: none for (A). For (B), explicitly waiting on a design discussion with the user before writing any code - do not implement the LLM-driven source-selection mechanism without their sign-off on the approach.
OPEN_FINDINGS: none open
FILES_IN_FLIGHT: app/pipeline/faostat.py, app/warehouse/schema.py, migrations/versions/9bfa1484a1d0_raw_faostat_records.py, scripts/load_faostat_production.py, tests/unit/test_faostat.py, tests/integration/test_faostat_upsert.py, docs/BUILD-LOG.md, docs/STATE.md are all written, tested, and live-verified but NOT YET COMMITTED as of this STATE.md write - commit immediately after.
