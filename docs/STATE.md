CURRENT_STEP: 3
CURRENT_ROLE: Software engineer (backend)
ITERATION: 1
GATE_STATUS: in_progress
LAST_COMPLETED: normalize_baci_rows (third normalizer in app/pipeline/normalize.py) and D9 check C (app/report/mismatch.py) both built and live-verified — the two follow-ups flagged at the end of the prior BACI-ingestion unit. normalize_baci_rows derives flow from which side of the exporter/importer pair India (699, same code as Comtrade) is on, and sets basis='FOB' for every row regardless of flow direction (BACI's own CEPII-adjusted methodology, deliberately different from Comtrade/DGCIS's CIF-for-imports convention). compute_check_c implements §10's exact formula; refactored the DGCIS-total-summing logic shared with check A into _sum_partner_totals. Real live run: normalized all 150 real BACI records (real FX rates from Frankfurter), computed check C for both flows against HS6 120791 - import 2022 gap=11.61% (quiet, a real tight independent cross-check), export 2022 gap=43.44% (warning), export 2023 gap=9.67% (quiet), with correct SkippedCheck results for years with real ZERO/NOT_REPORTED data (not bugs). All three D9 checks (A, B, C) are now real, tested, and live-verified for the canonical scenario for the first time. 520 tests passing (+7), mypy app (59 files)/ruff/black clean. This closes build sequence item 6 (BACI) as a real, complete, end-to-end capability.
NEXT_TASK: No single forced next task - items 4 (DGCIS) and 6 (BACI) are both now real, complete, multi-country/multi-source capabilities for the canonical scenario, with all three D9 mismatch checks live. Real remaining gaps, roughly in priority: (1) real HS 120791 duty-rate and regulatory-note citations from the user (mechanisms built/tested since early in this build, only real curated data missing - the single most user-blocked item at this point); (2) analytics_monthly_current_year writer (D15, current-year month-wise section) - not started, needs DGCIS's monthly report path investigated (separate from the annual report already built, similar live-investigation effort to what the annual report needed); (3) HS17 BACI file (~795MB) for 2020-2021 coverage (HS22 only covers 2022-2024) - self-contained, no user input, lower priority than D15; (4) Agmarknet (item 7, needs a data.gov.in API key from the user, blocks landed_cost's domestic_price/margin fields); (5) re-running report/facts.py + report/narrative.py end-to-end against all this new real breadth (rankings/HHI/check A/B/C/coverage are all richer now than when facts.py was first built and verified) would be a good, cheap sanity check before moving to a new build area, even though facts.py's own code hasn't changed.
BLOCKED_ON: none for (2)/(3)/(5). (1) and (4) need user-provided input.
OPEN_FINDINGS: none open
FILES_IN_FLIGHT: none
