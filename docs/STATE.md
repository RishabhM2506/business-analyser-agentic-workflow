CURRENT_STEP: 3
CURRENT_ROLE: Software engineer (backend)
ITERATION: 1
GATE_STATUS: in_progress
LAST_COMPLETED: Build sequence item 8, part 1 — app/pipeline/normalize.py built, then two real bugs found and fixed while designing mismatch.py against real data (both would have silently corrupted check B): (1) normalize_comtrade_rows used raw partner_code unconditionally, which stored India as its own trade partner for every partner-role (role="partner") row — fixed to resolve whichever side isn't India; (2) reporter-role and partner-role rows for the same (partner, flow) pair collided on normalized_trade_flows' unique key and silently overwrote each other — fixed by splitting dataset_version into comtrade-mirror-reporter-v1 / comtrade-mirror-partner-v1. Both encoded as permanent regression tests. Re-normalized all 363 real Comtrade rows + 5 real DGCIS rows for HS6 120791; verified via psql that Turkey (792) now shows India's own claim and Turkey's own claim as separate, correctly-labeled rows (e.g. 2020 import/reporter-role ₹344.71cr vs 2020 export/partner-role ₹68.84cr) — the real pair check B needs. 418 tests passing (+10 total this unit), mypy/ruff/black clean.
NEXT_TASK: Build sequence item 8, part 2 — app/report/mismatch.py's compute_checks() for check A (dgcis_total vs comtrade-mirror-reporter-v1 total at partner_country_code='0', §10) and check B (dgcis_import per partner vs comtrade-mirror-partner-v1 export for that partner, §10's 15%/40% severity bands + sign-flip-YoY untrustworthy rule). Real normalized data for both sources and both Comtrade query shapes now exists for HS6 120791/Turkey, so both checks are directly computable and verifiable against real numbers. After that: coverage_gate.py and unit_consistency.py (also item 8), which report/mismatch.py and the eventual report assembly will depend on for gating.
BLOCKED_ON: none — buildable now. Still open, not currently blocking: no real HS 120791 duty rates recorded (mechanism verified; citation pending from user); Agmarknet data.gov.in API key (blocks item 7); DGCIS's monthly national-total path (D15), general HS6->HS8 discovery mechanism, and full ~250-country rate-limit tuning still open within item 4; BACI not yet started (item 6); DGCIS FY vs Comtrade CY period alignment for mismatch checks is an approximation (DGCIS FY"2022-2023" normalized to period_month=2022-01-01, same year as Comtrade CY2022) — flagged for review when building mismatch.py, not yet reconciled against DGCIS's actual fiscal-year definition (April-March).
OPEN_FINDINGS: none open
FILES_IN_FLIGHT: none
