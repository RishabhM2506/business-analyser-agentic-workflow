CURRENT_STEP: 3
CURRENT_ROLE: Software engineer (backend)
ITERATION: 1
GATE_STATUS: in_progress
LAST_COMPLETED: Built and live-verified the user-directed dynamic source-relevance mechanism ("today poppy seed, tomorrow cement... decided by llm") - a hybrid design (HS-chapter rule primary, LLM fallback for a narrow boundary set) chosen via discussion before any code was written, per the user's own explicit request. New app/report/source_relevance.py (is_agriculture_relevant: chapters 01-24 -> True with no model call; chapters 50-53, real agricultural raw materials outside 01-24 like MSP's own "Cotton (Medium Staple)" row, -> one real MODEL_UTILITY call; everything else -> False with no model call). Wired the 3 new sources (Agmarknet, MSP, FAOSTAT - previously standalone raw ingestion with zero report-layer consumer) into app/report/facts.py as new MandiPriceFact/MspFact/InternationalProductionFact sections, each with a status of OK/NOT_FOUND/NOT_APPLICABLE - a real, deliberate new distinction (NOT_FOUND = source relevant but no matching row; NOT_APPLICABLE = the question doesn't apply to this commodity at all). assemble_facts gained a required include_agriculture_sources: bool param, computed once by the caller (app/main.py's post_trade_report route) via is_agriculture_relevant - assemble_facts itself stays fully deterministic, no model call inside it. Built a heuristic (not curated-crosswalk) commodity-name matcher (_commodity_name_matches/_normalize_commodity_text) since no HS6-to-source-commodity-name crosswalk exists and real names don't line up exactly (FAOSTAT's "Poppy seed" vs. this pipeline's taxonomy "Oil seeds; poppy seeds, whether or not broken") - flagged as a heuristic, not guaranteed-correct. app/models.py gained bool-field support in MockLLM's generic structured-output builder (first plain-bool schema field in this codebase). Live-verified against the real Gemini model for all 3 real cases: poppy seeds (chapter 12) -> True, no model call; portland cement (chapter 25) -> False, no model call - the literal "tomorrow cement" case confirmed working end to end through the full assemble_facts call, not just the classifier in isolation; raw cotton (boundary chapter 52) -> True via a real model call that genuinely fired. 21 net new tests (18 new + 3 existing call sites/fixtures fixed for the new required Facts fields). 634 tests passing (was 613). mypy (65 files)/ruff/black clean.
NEXT_TASK: Not yet committed - commit everything from LAST_COMPLETED above first. After that, nothing further is self-directed pending from the user's latest message; everything explicitly requested (BCD verification confirmed, Tier-2 investigation reported, FAOSTAT built, Horticulture Varieties retried and reported, dynamic source-relevance discussed and built, 3 new sources wired into the report) is done. If the user wants to continue: (a) DGFT/CBIC/FSSAI remain REFERENCE-ONLY (no public APIs) - manual curation via scripts/record_regulatory_note.py is the only path if a specific product needs one of these cited; (b) the commodity-name matching heuristic in facts.py is real but unverified beyond poppy seeds/cotton - broader real-world testing across more commodities would strengthen confidence; (c) Agmarknet's raw_agmarknet_prices table only has 10 test rows for "Green Peas" right now - a real bulk ingestion run (scripts/run_agmarknet_ingestion.py) for whatever commodities matter would make the mandi_price section actually useful beyond the current near-always-NOT_FOUND state; (d) horticulture varieties dataset (real API works, but returns yearly certification counts not variety names) was confirmed not useful for query normalization - no further action needed there.
BLOCKED_ON: none
OPEN_FINDINGS: none open
FILES_IN_FLIGHT: app/report/source_relevance.py, prompts/agriculture_relevance.md, app/report/facts.py, app/main.py, app/models.py, tests/unit/test_source_relevance.py, tests/unit/test_commodity_name_matching.py, tests/integration/test_agriculture_facts.py, tests/integration/test_facts.py, tests/unit/test_narrative.py, docs/BUILD-LOG.md, docs/STATE.md are all written, tested, and live-verified but NOT YET COMMITTED as of this STATE.md write - commit immediately after.
