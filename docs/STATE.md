CURRENT_STEP: 3
CURRENT_ROLE: Software engineer (backend)
ITERATION: 1
GATE_STATUS: in_progress
LAST_COMPLETED: User-directed unblocking round (2026-08-24), items 1-2 of 5 done. (1) HS 12079100 ICEGATE evidence: real live re-attempt against the Trade Guide search form (session cookie established, real 405 on GET confirming the endpoint, but the actual POST returned real HTTP 500 across 4 varied attempts) - recorded all 4 duty components (BCD/AIDC/SWS/IGST) as NOT_VERIFIED via scripts/record_duty_rate.py with a full evidence trail in notes (never attributed the user's undifferentiated "20%" to a specific component, per their own explicit caution and the evidence-first rule); wrote a regulatory note for HS6 120791 via a new scripts/record_regulatory_note.py (new writer for a previously-write-less table) documenting the same, plus a live CBIC cross-check (real connection reset). (2) Agmarknet ingestion: app/pipeline/agmarknet.py (new) built and live-verified against the real API with the user's real key (stored only in local, gitignored .env; Settings.agmarknet_api_key added, .env.example/tests/conftest.py/CI updated with placeholders). Two real, load-bearing findings: httpx's default User-Agent is silently black-holed by this API (fixed with an explicit honest UA); the real rate-limit shape is HTTP 200 with {"error": "Rate limit exceeded"}, not a 4xx/5xx. Confirmed, twice, a real structural data gap: zero Agmarknet records for poppy seeds under 13 name variants and 4 real licensed-poppy districts - poppy is a Central Bureau of Narcotics licensed-procurement crop, outside open APMC trading. 17 new tests (13 unit, 4 integration). Real end-to-end run upserted 10 real rows. Also fixed one stray pre-existing lint issue in tests/integration/test_monthly_current_year.py caught by the first-ever repo-wide ruff/black check this session. 571 tests passing (+17 since D15 closed at 554). mypy app (61 files)/ruff/black clean.
NEXT_TASK: Continue the user's 2026-08-24 unblocking round, items 3-5 of 5, all pre-approved by the user in that same message (no further permission needed): (3) BACI HS17 file for 2020-2021 historical coverage - inspect the existing BACI integration (app/pipeline/baci.py, scripts/load_baci_zip.py) and extend it, add/update tests, verify coverage, report exact years/records added. (4) A full real 2026 monthly DGCIS ingestion run using the now-closed analytics_monthly_current_year writer - use scripts/run_dgcis_country_batch.py's sibling monthly path (fetch_year_monthly/upsert_monthly_records in app/pipeline/dgcis.py), correctly distinguishing NOT_YET_PUBLISHED from a real zero, report success/failure per month. (5) The approved "posta dana" vernacular-search fix - full plan already exists and is approved at ~/.claude/plans/drifting-plotting-marshmallow.md (app/search/normalize.py new module, prompts/normalize_query.md new prompt, app/search/service.py gated retry, app/models.py one new MockLLM branch, 3 new/extended test files) - implement exactly as planned, verify against the real Gemini-backed app per the plan's own rollout-verification section. After all 5: run full suite + mypy + ruff + black, report final test count, every changed file, remaining blockers, and the VERIFIED/NOT_VERIFIED/NOT_FOUND/NOT_YET_PUBLISHED distinctions per the user's final acceptance criteria.
BLOCKED_ON: none for items 3-5 - all pre-approved by the user. HS 120791's real duty-rate/regulatory-note values remain NOT_VERIFIED (ICEGATE unreachable this session) and Agmarknet has a real, confirmed NOT_FOUND for poppy seeds specifically - both are now closed-out findings, not open blockers on further work.
OPEN_FINDINGS: none open
FILES_IN_FLIGHT: none (items 1-2 committed; items 3-5 not yet started)
